{
  "name": "dvcr-functions-video",
  "description": "DVCR Cloud Functions — export highlight (ffmpeg isolé)",
  "scripts": {
    "deploy": "firebase deploy --only functions:video"
  },
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "dependencies": {
    "ffmpeg-static": "^5.3.0",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  }
}
