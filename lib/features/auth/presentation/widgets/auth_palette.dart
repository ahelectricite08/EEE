import 'package:flutter/material.dart';

/// Visual tokens for Auth screens — parity with legacy login/register.
abstract final class AuthPalette {
  static const bg = Color(0xFFF5F2E9);
  static const surface = Color(0xFFFFFFFF);
  static const border = Color(0xFFDDD8CC);
  static const text = Color(0xFF173C31);
  static const muted = Color(0xFF6E776F);
  static const red = Color(0xFFBA203C);
  static const gold = Color(0xFFC8A436);
}
